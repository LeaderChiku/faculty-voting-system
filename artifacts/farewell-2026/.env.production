# Production environment variables
# Vite bakes VITE_* variables directly into the compiled bundle at build time.
# This URL is used by customFetch to call the Render backend.
VITE_API_BASE_URL=https://faculty-voting-system.onrender.com
